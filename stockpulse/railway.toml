[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python database/init_db.py && python scheduler.py'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[environments.production.deploy]
numReplicas = 1
